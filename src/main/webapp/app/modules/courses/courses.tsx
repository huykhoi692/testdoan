import React from 'react';
import { Typography } from 'antd';

const { Title, Paragraph } = Typography;

const Courses = () => {
  return (
    <div style={{ padding: '2rem' }}>
      <Title>Courses</Title>
      <Paragraph>This is the courses page. Content will be added soon.</Paragraph>
    </div>
  );
};

export default Courses;
